/// Manage the leveling of adventurer and players
/// Same exp chart for both but players only get exp a % of auto kills, and per tap.

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelManager : MonoBehaviour
{
    //enters level, return required exp
    int requiredEXP(int level)
    {
        int requiredExpToLevel = 0;
        return requiredExpToLevel;
    }
}
